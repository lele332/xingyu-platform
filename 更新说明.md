# 星屿 · 更新说明

更新日期：2026-08-14　版本：v20260814.21

---

## ★ 天气功能：直连 GitHub，手机端也能用中国天气网实时数据

- **怎么做到的**：平台每 30 分钟自动抓取中国天气网 42 个主要城市的实时天气，把 JSON 提交到仓库的 `data/weather/`（桌面端由 Windows 计划任务「星屿天气更新」执行；GitHub **Actions 云端工作流已上线**，每 30 分钟自动运行，电脑关机也能更新——已实测手动触发运行成功）。
- **前端直连 GitHub**：手机 / 远程访问（原生后端不在线）时，天气页自动改为**直连 GitHub** 拉取这些定时发布的天气 JSON——同样能看到中国天气网的真实数据（实时温度、AQI、预警、16 项生活指数、7 天预报）。
- **数据源优先级**：桌面端用本机原生后端（最实时）→ 手机/远程用直连 GitHub（约 30 分钟更新一次）→ 都不行再降级 Open-Meteo。
- 覆盖 42 个主要城市（省会 + 一线 + 热门城市）；若需要加入你的常住城市，告诉我城市名即可。

---
## ★ 天气功能：接入中国天气网实时数据（对齐中国天气 App）

- **接入中国天气网官方实时数据**（本机原生后端代理，桌面端直接可用）：实时温度、天气现象、风向风力、湿度、气压、能见度、PM2.5、空气质量指数（AQI）。
- **空气质量分级**：实时 AQI 数值 + 「优 / 良 / 轻度污染…」分级，颜色随等级变化。
- **气象预警**：出现预警时顶部显示醒目横幅（雷电 / 暴雨 / 大风等 + 黄/橙/红等级），含官方预警详情与发布时间。
- **生活指数**：像中国天气 App 一样提供 **16 项生活指数**——穿衣、紫外线、感冒、运动、洗车、晾晒、雨伞、防晒、空调、旅游、晨练、钓鱼、过敏、交通、中暑等，每项带建议描述（悬停可见）。
- **7 天预报增强**：每天显示天气图标 + 现象文字 + 最高/最低温 + 风向风力。
- **我的城市 + 城市搜索**：沿用「设为我的城市」固定常住地；搜索支持中国天气网城市库（如「长沙 / 丽江」按中文名即可）。
- **多端兜底**：桌面端用中国天气网完整数据；手机端 / 远程访问（原生后端不在线时）自动降级到 Open-Meteo，天气功能始终可用。

---
## ★ 天气功能重新设计：我的城市 + 智能出行提醒

- **「我的城市」固定位置**：你所在的位置长期不变，不用每次切换。在天气页点「📍 设为我的城市」，以后打开应用都会优先加载这个城市（已存到本机，永久记住）。
- **显示更实用**：城市名旁有「我的城市」专属徽标；新增今天**最高/最低温**一览；日出日落下方直接显示**降水概率**与**紫外线指数**。
- **智能出行提醒**：根据体感温度、降水概率、紫外线、风速、温差、湿度，自动生成今日提醒卡片（如「天气炎热注意防暑」「紫外线强外出防晒」「有雨带伞」等），穿什么、带不带伞一看便知。
- **7 天预报更直观**：每天预报下方新增**降水概率**，一眼看出哪天有雨。
- 城市列表里「我的城市」自动置顶并带 ⭐，方便快速回到常住地。

---
## 一、开屏视频：双击图标即自动出声 + 全屏

- **双击桌面「星屿」图标后，开屏视频会自动带声音播放，并自动全屏**，无需再点「开启声音」、无需手动全屏。
- **实现方式**：启动器改用独立浏览器配置目录 `edge-profile`（不再受"Edge 已打开导致参数失效"影响），并加入 `--autoplay-policy=no-user-gesture-required`（自动出声）与 `--start-fullscreen`（自动全屏）。
- **数据自动迁移**：首次双击启动时，会把原有浏览器里的应用数据（课程、任务、番茄钟、考试日程、励志语录设置等）自动迁移到 `edge-profile`，不丢数据。
- **声音设置仍然生效**：在「设置」里关闭开屏声音，或选择「无声音」，则开屏保持静音，不打扰。

> 提示：首次迁移后，请统一通过桌面图标进入应用；之前如果另外在普通 Edge 标签页打开过，那是旧数据副本。

---

## 二、番茄钟实时字幕：可移动、不再遮挡

- 字幕条现在是**悬浮玻璃条**，可以在屏幕上**按住并拖动**到任意位置。
- 位置会自动记忆，下次打开仍停在原处。
- 修复了之前"字幕遮挡/挤压页面其他文字"的问题（原来的卡片动画会干扰定位，现已把字幕移到独立层级）。

---

## 三、考试日程 · 日历重新设计

- 日历顶部新增**月份概览**："本月日程 / 未来日程 / 距「××」X 天"。
- 新增「**回到今天**」按钮，一键跳回当前月并选中今天。
- 选中某天的日程会显示**倒计时徽章**（如"还有 12 天"）。

---

## 四、倒计时卡片：加动画，不再空

- "下一个关键时刻"卡片重做：
  - 中央**大号倒计时圆环**，随日期临近自动填充；
  - **数字滚动动画**（从 0 数到剩余天数）；
  - **进度条**显示时间推进程度；
  - **背景流光动画**，视觉更精致。

---

## 五、考试日程功能（此前提到的完整功能）

- 侧边栏新增「考试日程」入口，含「日程列表」与「日历」两个视图。
- 支持添加考试 / 作业截止 / 重要日程 / 重要日子，记录日期、时间、备注。
- 列表带大倒计时卡片、类型与状态筛选、标记完成 / 编辑 / 删除。
- 数据本地持久化，设置一次即可长期使用。

---

## 六、无障碍与性能

- 为 8 个无标签输入框补充了 `aria-label`（任务/成绩过滤、AI 输入框、城市搜索、文献筛选、导入文件等）。
- 移动端图标触控目标提升到 44px。
- 全部外部脚本改为 `defer` 加载，页面打开更快。


## 五、沉浸式专注场景：字幕条可拖动、位置记忆

- 进入「开始专注」后的**沉浸式全屏场景**里，播放励志语录时同样会显示**实时逐字字幕**。
- 字幕条现在是**可拖动悬浮玻璃条**：按住即可拖到任意位置，放开后自动记忆，下次进入仍停在原处（与主介面番茄钟一致）。
- 沉浸式场景字幕采用独立的半透明深色玻璃样式，与深色背景自然融合，不遮挡计时圆盘与场景画面。

## 六、字幕改为「实时」：显示音频实际内容

- 之前的字幕只显示**语录标题**，现在改为显示**完整励志文案**（音频里实际诵念的内容），逐字即时浮现。
- 番茄钟卡片与沉浸式专注场景的字幕都已同步更新，播放到哪一句就显示到哪一句。
- 每段语录都配好了对应的完整字幕文案；以后新增音频时，只需在语录列表里填上 `subtitle` 字段即可自动生效。

## 七、开屏声音：新增科技氛围音源 + 消除播放延迟

- **新增内置开屏音源「星屿 · 科技氛围」**：一段约 12 秒的合成器氛围配乐（和弦 pad + 流动琶音 + 开场渐强），与开屏时长匹配，现已设为默认开屏声音。
- 在「设置 → 开屏欢迎声音」里可随时切换回励志语录，或使用你上传的 MP3/MP4。
- **修复双图标声音延迟**：开屏声音改为在页面 `DOMContentLoaded` 时立即启动（原来要等所有资源加载完的 `load` 事件），并预加载音讯，双击图标后声音响应明显更快。

## 八、字幕改为「跟随播放进度」实时显示

- 字幕不再按固定速度打字，而是**跟随音频实际播放位置**逐字显现：播放到哪一句就显示到哪一句，与语音同步。
- 番茄钟卡片与沉浸式专注场景均生效。若音频尚未取得时长，会先显示全文兜底。
- 说明：当前为「按音频播放进度同步显示已配好的文案」。若要做到对任意/上传音频的**真正自动语音识别**，需要接入后台语音识别引擎（当前本地环境无网络，无法部署），这部分可后续在联网环境接入。

## 九、集成「工具箱」到平台

- 将 ToolKnit 桌面版（多功能工具箱）网页前端构建为静态站点，并作为平台新增的「**工具箱**」视图嵌入（侧边栏导航 + 全屏 iframe，与「棱镜艺境」等场景一致）。
- 内置 40+ 工具，覆盖 PDF、图片、音视频、文本、计算器、AI 文档等。**纯前端工具**（文本统计/格式化、密码生成、体脂率计算、打字测试、图标生成、部分 PDF/图片处理等）可直接在网页内使用。
- 说明：此工具箱为桌面（Tauri）应用，网页内为浏览器回退模式。依赖原生运行时的工具（音视频格式转换、Whisper 语音转写、部分 PDF 加密需要 ffmpeg/qpdf）在网页内会显示「未安装」并不可用；如需完整能力，可安装 ToolKnit 桌面版 exe。

## 十、工具箱：独立分组 + 全屏界面修复

- 工具箱不再放入「灵感画廊」，改为侧边栏**独立「实用工具」分组**，拥有专属入口与界面。
- **修复了「用不了」的问题**：此前工具箱 iframe 未应用全屏样式，被压缩成约 300×150 的小框导致无法操作。现已让工具箱与沉浸式场景一样**全屏显示**（`calc(100vh - 62px)`，移动端再减底部导航），工具可正常打开使用。

## 十一、工具箱界面与侧边栏细节优化

- **移除工具箱右上角无效的窗口控制按钮**（最小化/最大化/关闭）：这些是桌面 Tauri 应用的控件，在平台内被嵌入为网页时无法生效，现已隐藏，避免误点与逻辑错误。
- **侧边栏分组标签的「[]」小标记已移除**：之前「灵感画廊」「实用工具」等分组的标题后会显示一个空括号记号，观感不佳，现在无内容的分组不再显示该标记，标题更干净。

## 十二、侧边栏图标重新设计：去掉丑陋的三点

- 之前的「工具箱」「灵感画廊」(棱镜艺境/星网/折页工坊/守护符) 以及「考试日程」在侧边栏显示的是三个水平圆点 `⋯` 占位图标，观感不佳。
- 现已为它们各设计**专属线性图标**（延续全平台统一的 20×20 圆角线性风格）：
  - 工具箱 → 工具箱图标（箱子 + 把手 + 旋钮）
  - 棱镜艺境 → 菱形棱镜
  - 星网 → 网络节点
  - 折页工坊 → 折页
  - 守护符 → 盾牌 + 对勾
  - 考试日程 → 日历

## 十三、工具箱：标注「仅桌面版」工具，摸清可用的边界

- 工具箱里有一部分工具依赖**本机文件/硬件**，在网页(平台内嵌)里无法运行。现在会在这些工具卡片右上角自动加一个**琥珀色「仅桌面版」徽标**，一眼就能看出哪些能用、哪些不能用。
- **网页可直接使用**(约 38 个)：PDF 合并/拆分/旋转/压缩/增强/转图像、AI 表格/润色/翻译/文档、文本统计/格式化、各类计算器、密码生成、配色提取、打字测试、长图拼接、BPM 测速等。
- **仅桌面版**(约 18 个)分三类：
  - **音视频/图片转换压缩**(8)：视频转换、音频转换、图片转换、图片压缩、音频剪辑、音频提取、视频取帧、视频转 GIF —— 依赖 ffmpeg。
  - **解密/识别**(2)：PDF 解密(需 qpdf)、音视频提取文字(需离线 Whisper 模型)。
  - **本机硬件/文件**(8)：整机概览、CPU 与内存、显卡与显示器、主板与固件、磁盘与健康、网络与设备、电源与传感器、AI 大文件清理 —— 需读取本机硬件，浏览器物理上无法实现。
- 下一步(可选)：为前 8 个「音视频/图片转换压缩」工具接入 **ffmpeg.wasm**(浏览器内运行 ffmpeg)，即可在网页里直接使用，无需桌面版。

## 十四、工具箱：8 个音视频/图片转换工具现可在网页直接使用（ffmpeg.wasm）

- **新增「网页版多媒体工坊」**：在浏览器本地用 ffmpeg.wasm 处理文件，**不上传服务器**。
- 以下 8 个原先「仅桌面版」的工具，现在点击卡片会直接打开网页版并可用：
  - 视频格式转换、音频格式转换、图片格式转换、图片压缩、音频提取、音频剪辑、视频取帧、视频转 GIF。
- 支持批量（转换/压缩类）、拖拽上传、实时进度条、一键下载；首次打开需加载 32MB 引擎（约几秒）。
- **仍仅桌面版**（浏览器无法读取本机硬件/模型）：PDF 解密、音视频提取文字（Whisper 模型）、整机概览、CPU 与内存、显卡与显示器、主板与固件、磁盘与健康、网络与设备、电源与传感器、AI 大文件清理。

## 十五、硬件信息工具：改为网页版「设备信息」（尽可能读取）

- 之前 7 个硬件类工具（整机概览、CPU 与内存、显卡与显示器、主板与固件、磁盘与健康、网络与设备、电源与传感器）在网页里只能提示「仅桌面版」。现已改为打开**网页版「设备信息」**，读取浏览器能提供的真实信息：
  - 整机概览：操作系统、浏览器、平台、语言、时区、在线状态。
  - CPU 与内存：逻辑核心数、估算内存。
  - 显卡与显示器：分辨率、色彩深度、像素比、屏幕方向、WebGPU 适配器。
  - 网络与设备：在线状态、网络类型、下行带宽、往返时延。
  - 电源与传感器：充电状态、电量、电池健康。
- **说明**：浏览器出于安全限制，**无法读取** CPU/显卡型号、主板、磁盘、传感器等本机硬件详情（这是所有浏览器的通用限制，非本工具缺失）。这些项会明确标注「需桌面版」。

## 十六、工具箱强制刷新 + 剩余「仅桌面版」清单

- **修复「改了没生效/仍显示仅桌面版」的缓存问题**：工具箱 iframe 加入版本参数，重启应用后强制加载最新版，确保上述改动生效。
- 随着「本机原生后端」上线（见第十七节），**PDF 解密、AI 大文件清理、7 个硬件信息工具**现均可在网页/平台内直接使用。
- 随着「音视频提取文字」接入本机离线识别引擎（见第十八节），**工具箱里已没有「仅桌面版」工具**，全部工具均可在平台上直接使用。


---

## 十七、工具箱：本机原生后端，解密 / 硬件 / 大文件清理网页可用

- **新增本机原生后端**（随桌面图标自动启动，监听 `127.0.0.1:8621`，仅本机可访问），让原先「仅桌面版」的几个工具在平台网页里也能真正使用：
  - **PDF 解密**：点击卡片进入「PDF 解密」页，拖拽/选择 PDF、输入打开密码即可解密并一键下载（本机用 pypdf 完成，文件不上传网络）。
  - **AI 大文件清理**：点击卡片进入「大文件清理」页，选择盘符/文件夹与大小阈值，即可列出本机大文件（跳过系统目录以加快速度）。
  - **本机设备信息**：7 个硬件工具现在读取到你电脑的**真实硬件**（CPU 型号、显卡、主板、磁盘、网卡、电池等），不再只是浏览器能提供的估算值。
- **启动方式**：双击桌面「星屿」图标会自动拉起原生后端，无需单独配置。若提示「本机原生服务未启动」，请完全退出应用后重新双击图标。


---

## 十八、音视频提取文字：接入本机离线识别引擎

- 「音视频提取文字」现在可在平台内直接使用，不再「仅桌面版」。
- **实现方式**：随桌面图标启动的本机原生后端内置 **faster-whisper（small 模型）**，在你的电脑上用 CPU 离线进行中英文语音识别，**音频不上传网络**。
- 点击工具箱「音视频提取文字」卡片进入网页版：上传音频/视频（MP3 / WAV / M4A / MP4 / MKV 等），选择语言（自动 / 中文 / English），即可转写。
- 转写完成后显示**带时间戳的分段文字**，并支持一键下载 **TXT（纯文本）**、**SRT（字幕）**、**JSON** 三种格式。
- 首次使用会自动加载本地识别模型（约 480MB，已在本地，无需联网下载）；识别在 CPU 上运行，短音频通常几秒内完成。
- 至此，工具箱内全部工具均可在平台上直接使用。

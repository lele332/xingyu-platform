# 星屿 · 统一设计与体验系统

> 本文件是品牌与界面的唯一事实来源。新增页面、弹窗、图表、AI 能力或移动端入口，必须复用这里的令牌与交互规则。

## 1. 品牌定位

- 名称：**星屿**
- 一句话定位：学生自己的学习操作系统。
- 核心承诺：把分散的课程、任务、笔记、专注与成长收拢在本地优先的一张“星图”里。
- 品牌人格：安静、可靠、轻盈、专业；不做娱乐玩具，也不做冷冰冰的管理后台。
- 品牌符号：**✦ 星屿星标**。用于应用图标、启动屏、侧边栏、锁屏、PWA 与分享卡片，不允许替换成其他星形或 emoji。
- 文案语调：直接、具体、克制。写“开始专注”，不写“立即开启极致沉浸体验”。

## 2. 视觉令牌

| 令牌 | 值 | 用法 |
|---|---|---|
| Starlight | `#6ee7ff` | 主强调、当前位置、焦点光晕 |
| Star Deep | `#38bdf8` | 按钮 hover、链接、可交互信号 |
| Abyss | `#071018` | 深色底 / 浏览器主题色 |
| Brand Gradient | `linear-gradient(135deg,#7dd3fc,#818cf8,#c084fc)` | 仅用于品牌标、主快捷按钮、移动端选中态；不做大面积装饰背景 |
| Radius | `22px / 16px / 12px` | 卡片 / 弹窗与主控件 / 输入框 |
| Control Height | `42px desktop / 48px mobile` | 所有可点击控件 |
| Motion | `260ms cubic-bezier(.32,.72,.16,1)` | 展开、抽屉、弹窗；只使用 transform/opacity |
| Feedback | `160ms` | hover、active、输入反馈 |

色彩层级遵循 60/30/10：深色或纸面为 60，灰阶卡片与文字为 30，星光渐变只承担 10。所有正文与关键数据对比度必须达到 AA；状态不允许只靠颜色区分，必须配合图标、文字或位置。

## 3. 全局交互规则

### 3.1 精简导航

- 核心导航只保留：**今日 / 课程作业 / 专注学习 / 学习笔记库 / AI 助手**。
- 天气、考试、文献、新闻、语音、成长、训练营、灵感画廊、工具箱统一进入“更多能力”。
- 移动端底部导航不超过 5 项，桌面端侧边栏不新增一级入口；新能力进入“更多能力”或相关二级页面。
- 当前位置必须同时有颜色、指示点和 `aria-current`/`.active`；扩展组被隐藏时，深链接与快捷键应自动展开。

### 3.2 高频路径

- 首页 Hero 下方固定 4 个高频动作：**新建任务 / 开始专注 / 新建笔记 / 问小星**。
- 每个主视图只保留一个主按钮；次要操作使用文本按钮或次级按钮。
- 全局搜索 `Ctrl+K`、快捷键 `Alt+1..9`、PWA shortcut 和 URL `?view=` 保持可用。

### 3.3 反馈与容错

- 所有保存、同步、AI 生成、导入导出、失败恢复必须使用 `window.showToast()` 给出轻量反馈。
- Toast 不打断当前流程；错误提示应说明影响和下一步，例如“本地功能不受影响”。
- 破坏性操作必须确认；非破坏性流程避免阻断。表单支持失焦校验，长表单尽量保留草稿。
- 可点击元素必须有 active 缩放、hover 状态和 `:focus-visible` 焦点环；键盘可用性不可降级。

### 3.4 渐进披露

- 首屏只展示“今天该做什么”。
- 高级设置、实验能力、扩展工具、AI 语音与灵感画廊默认折叠。
- 空状态必须解释价值并提供一个动作；加载态使用骨架；错误态提供重试。

## 4. 多端一致性

- 桌面：侧边栏为主导航，顶部搜索与时钟稳定可见。
- 移动：底部导航承载核心，侧边栏变为抽屉承载全部能力；触摸目标至少 48px。
- 全屏、锁屏、PWA、分享卡、桌面快捷方式使用同一品牌色与星标。
- 安全区使用 `env(safe-area-inset-*)`；底部内容预留移动导航空间。
- `prefers-reduced-motion` 下关闭装饰动画，只保留状态变化。

## 5. 落地文件

- `css/brand-system.css`：品牌令牌、导航、快捷操作、Toast、焦点与移动导航。
- `js/ux-system.js`：全局 Toast、高频动作、渐进导航、移动核心导航。
- `index.html`：应用壳层引用统一体验层。

## 6. 桌面宠物体验

- 宠物默认 Live2D 模式，无边框、无黑底、无方形“框”感；只有宠物本体和悬浮对话。
- 互动：底部悬停后出现工具栏；点击 ✦ 打开对话，输入可与本机 AI 代理对话；未配置 AI 时自动降级为本地兔子式建议。
- 支持 `L`（Live2D）与 `A`（AIRI 完整运行时）切换；AIRI 使用轻量模型并按需加载，避免冷启动假死。
- 透明窗口使用隐私色 chromakey（`RGB(1,2,3)`），不再用黑色抠像，避免模型暗部被抠穿。
- 宠物具备平台只读上下文（昵称、目标、任务、逾期、课程、专注、笔记标题、考试），不读取密钥、笔记正文、成绩明细。
- 本机长期记忆保存称呼、偏好、目标与学习节奏，最多 12 条；用户可一键清空。
- 主动陪伴只在逾期、今日到期、专注不足或长时间无互动时触发，并使用长冷却；轻量气泡可点击展开对话。
- 宠物对话正文遵守聊天气泡规则：最多 3 句话、给出可执行下一步、不编造本地数据。

## 7. 问题反馈系统

- 自动巡检基于 5 / 15 / 60 分钟滚动趋势，只上报“持续 / 严重”问题；单次轻微卡顿、非关键资源 404、Autoplay/Permission 等噪声只进入原始指标。
- 页面隐藏、全屏、专注、视频播放和输入框聚焦时自动提醒顺延；不抢走用户当前操作。
- 主界面帧率问题需要持续低帧；长任务需要连续采样命中或单次极长才记录；非噪声 JS 错误例外，可高优先级上报。
- 自动报告默认 15 分钟冷却；安静模式 60 分钟；同签名问题 2 小时冷却，重复问题不再刷屏。
- 反馈面板展示 5/15/60 分钟趋势、噪声过滤数量与最近自动报告；手动反馈入口不受限制。

## 8. 图标语言（v2）

- 统一 24px 网格、1.75px 描边、圆头端点；语义先于装饰，不叠加渐变、阴影、玻璃、碎笔与跳动动画。
- 侧边栏图标使用 18px 显示尺寸，hover/active 只做透明度与极轻微位移；移动端底部导航 19px。
- 图标主题只保留 `classic`（星屿线性）与 `mono`（单色线稿）；旧 localStorage 值自动回退为 `classic`。
- 设置页使用真实 SVG 预览；新增图标必须先通过小尺寸可识别、笔画一致、无碎笔、语义清晰的 audit。

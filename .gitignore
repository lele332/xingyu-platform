# 敏感配置：包含个人 API Key，禁止提交到 GitHub！
js/local-config.js

# 其他
*.log
.DS_Store
node_modules/
tests/artifacts/
__pycache__/
*.pyc

# 隐私 / 运行 / 开发产物（不随仓库发布）
.workbuddy/
.impeccable/
news/
scripts/
xingyu-app.pyw
# xingyu.ico 2026-09-14 移出忽略名单：index.html 与 sw.js CORE 都引用它，
# 之前被忽略导致线上 favicon / SW 预缓存 404（内容与 xingyu-apple.ico 完全一致）
assets/intro-720.mp4

# 本地运行 / 浏览器配置 / 数据 / 大文件（不随仓库发布到 GitHub Pages）
edge-profile/
native/
work/
bak-pomo/
data/backups/
focus-scenes/node_modules/
focus/node_modules/
# VoxCPM 模型权重缓存（ModelScope，体积大，不随仓库发布）
models/
# 本机 AI 密钥 / 反馈报告（绝不提交）
data/ai-key-local.txt
data/feedback/

# ---- 体积炸弹 / 临时产物（git add -A 的保险丝）----
# 浏览器用户数据目录：761M 的 WebView2 缓存 + 宠物/调试 profile，绝不入库
webview-data/
edge-profile/
# AIRI 同源镜像（329M 第三方前端产物，可重新拉取，不入库）
airi/
# 备份与一次性诊断脚本
_bak/
_diag*.py
_micdiag.py
*.lnk
assets/intro-720.mp4.bak-apple
assets/intro-720.orig.mp4
# 抓取/诊断留档
fetch_news_stderr.txt
_audio.txt
_priv.txt
_sysinfo.txt
_wtest.txt
_tts_test.mp3
data/access-token.txt
data/platform.db
data/platform-state.json


# ---- 2026-09-14 补：本地后端服务 / 模型权重 / 大媒体 / 开发产物 ----
# 这些只在本机跑，GitHub Pages 用不到，且体积会把仓库撑爆（合计 >150MB）。
agent-service/
focus-supervisor/
synapse-run/
voxcpm/
orb/
backups/
assets/scenes/
assets/vrm/
assets/live2d/
assets/avatars/*.broken
data/agent-memory/
data/server-log.txt
data/localstorage-migration.json
tools/__pycache__/

# 运行时生成：局域网码随当前网络变化，不入库（永久码 assets/xingyu-qrcode.png 要入库）
assets/lan-access-qr.png

# 文档截图（只在交接说明里用，站点不引用）
assets/synapse-*.png
assets/vox-*.png
assets/voice-fixed.png

# 开发 / 调试产物
*.bak-*
work-inspect.js
tests/*.png

# 一次性调试 / 诊断脚本与截图产物（tests/ 根只留 npm run 驱动的正式用例）
tests/dev/
tests/artifacts/

# 本机代码同步任务的运行日志（tools/sync_push.py）
data/sync-log.txt
